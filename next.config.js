/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ConfiguraÃ§Ã£o bÃ¡sica de PWA pode ser feita via plugins como next-pwa
  // Por enquanto, garantimos que o Next.js nÃ£o barre as rotas offline
};

module.exports = nextConfig;

